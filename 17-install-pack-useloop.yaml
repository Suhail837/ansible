- name: install packages using loops 
  hosts: display
  become: yes
  tasks:
  - name: install
    ansible.builtin.package:
      name: "{{ item }}"
      state: install

    loop:
    - mysql
    - nginx
    - zip